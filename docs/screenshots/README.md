# Screenshots

The three images the project README embeds. Replace them whenever the UI
changes enough that they stop being true.

| File | What it should show |
| --- | --- |
| `overview.png` | `/` — the stat tiles, the server cards with their live badge, and the latest rule activity |
| `rule-builder.png` | `/rules/new` or `/rules/:id/edit` — the step nav, the Setup card, and "In plain words" beside it |
| `rule.png` | `/rules/:id` — the KPI strip, the tabs, recent activity and the outcomes panel |

Taken at 1920×920 in dark mode, signed in as an administrator, with at least
one connected server and a rule that has fired a few hundred times — an empty
install photographs badly.
